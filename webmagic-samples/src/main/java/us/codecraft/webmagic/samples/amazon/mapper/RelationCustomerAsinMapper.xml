<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="us.codecraft.webmagic.samples.amazon.dao.RelationCustomerAsinDao">
    <resultMap id="RelationCustomerAsinMap" type="RelationCustomerAsin">
        <id column="rca_id" property="id" />
        <result column="bec_code" property="customerCode" />
        <result column="bas_code" property="siteCode" />
        <result column="saa_asin" property="asin" />
        <result column="saa_root_asin" property="rootAsin" />
        <result column="rca_status" property="status" />
        <result column="rca_create_time" property="createDate" />
        <result column="rca_update_time" property="updateDate" />
    </resultMap>

    <insert id="add" parameterType="RelationCustomerAsin" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO relation_customer_asin (
          bec_code,
          bas_code,
          saa_asin,
          saa_root_asin,
          rca_status
        ) VALUES (
          #{customerCode},
          #{siteCode},
          #{asin},
          #{rootAsin},
          #{status}
        )
    </insert>

    <insert id="addAll" parameterType="java.util.List" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO relation_customer_asin (
            bec_code,
            bas_code,
            saa_asin,
            saa_root_asin,
            rca_status
        ) VALUES
        <foreach collection="list" item="item" index="index" separator=",">
            (
                #{item.customerCode},
                #{item.siteCode},
                #{item.asin},
                #{item.rootAsin},
                #{item.status}
            )
        </foreach>
    </insert>

    <update id="updateById" parameterType="RelationCustomerAsin">
        UPDATE
          relation_customer_asin
        SET
          bec_code = #{customerCode},
          bas_code = #{siteCode},
          saa_asin = #{asin},
          saa_root_asin = #{rootAsin},
          rca_status = #{status}
        WHERE
          rca_id = #{id}
    </update>

    <delete id="deleteById" parameterType="int">
        DELETE FROM relation_customer_asin
        WHERE rca_id = #{id}
    </delete>

    <select id="findByCustomer" resultMap="RelationCustomerAsinMap">
        SELECT
          rca_id,
          bec_code,
          bas_code,
          saa_asin,
          saa_root_asin,
          rca_status,
          rca_create_time,
          rca_update_time
        FROM
          relation_customer_asin
        WHERE
          bec_code = #{0}
    </select>

    <select id="findByCustomerAndAsin" resultMap="RelationCustomerAsinMap">
        SELECT
          rca_id,
          bec_code,
          bas_code,
          saa_asin,
          saa_root_asin,
          rca_status,
          rca_create_time,
          rca_update_time
        FROM
          relation_customer_asin
        WHERE
          bec_code = #{0} AND saa_asin = #{1}
    </select>
</mapper>