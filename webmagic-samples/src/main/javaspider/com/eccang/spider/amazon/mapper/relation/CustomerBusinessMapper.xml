<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.eccang.spider.amazon.dao.relation.CustomerBusinessDao">
    <resultMap id="CustomerBusinessMap" type="CustomerBusiness">
        <id column="rcb_id" property="id" />
        <result column="bec_code" property="customerCode" />
        <result column="besb_code" property="businessCode" />
        <result column="rcb_max_data" property="maxData" />
        <result column="rcb_use_data" property="useData" />
        <result column="rcb_extra" property="extra" />
        <result column="rcb_create_date" property="createTime" />
        <result column="rcb_update_date" property="updateTime" />
    </resultMap>

    <insert id="addOne" parameterType="Business">
        INSERT into relation_customer_business (
          bec_code,
          besb_code,
          rcb_max_data,
          rcb_use_data,
          rcb_extra
        ) VALUES (
          #{customerCode},
          #{businessCode},
          #{maxData},
          #{updateDate},
          #{extra}
        )
    </insert>

    <insert id="addAll">
        INSERT into relation_customer_business (
          bec_code,
          besb_code,
          rcb_max_data,
          rcb_use_data,
          rcb_extra
        ) VALUES
        <foreach collection="list" index="index" item="item" separator=",">
          #{item.customerCode},
          #{item.businessCode},
          #{item.maxData},
          #{item.updateDate},
          #{item.extra}
        </foreach>
    </insert>

    <update id="updateOne">
        UPDATE
          relation_customer_business
        SET
          rcb_max_data = #{maxData},
          rcb_use_data = #{useData},
          rcb_extra = #{extra}
        WHERE
          bec_code = #{customerCode} AND besb_code = #{businessCode}
    </update>

    <delete id="deleteOne">
        DELETE FROM relation_customer_business
        WHERE bec_code = #{0} AND besb_code = #{1}
    </delete>

    <select id="findAll" resultMap="CustomerBusinessMap">
        SELECT * FROM relation_customer_business
    </select>

    <select id="findByCustomerCode" resultMap="CustomerBusinessMap">
        SELECT * FROM relation_customer_business
        WHERE bec_code = #{0}
    </select>

    <select id="findByCode" resultMap="CustomerBusinessMap">
        SELECT * FROM relation_customer_business
        WHERE bec_code = #{0} AND besb_code = #{1}
    </select>
</mapper>